---
layout: base.liquid
---

<article class="mt-4 mb-5">
  <h1 class="mb-2">{{ title }}</h1>
  <p class="text-muted">
    {{ date | date: "%B %d, %Y" }}
    {% if category %} | <span class="badge badge-info">{{ category }}</span>{% endif %}
  </p>

  {% if image %}
    <div class="text-center">
      <img src="{{ image }}" alt="{{ title }}" class="img-fluid rounded mb-4" style="max-width: 600px; height: 280px;">
    </div>
  {% endif %}

  <div class="content">
    {{ content }}
  </div>
</article>